1-download from https://cloud-images.ubuntu.com/minimal/releases/
2-mount the squashfs file
3- use rsync to copy the files from the squasfs the disk
sudo rsync -av -A -X /media/mint/disk/ /media/mint/2fa5f7a5-27ef-4ff9-97f0-ade7c9c593c6
4-cd into the disk and open supersuer terminal and past

mount -t proc /proc proc/
mount -t sysfs /sys sys/
mount --rbind /dev dev/
mount --rbind /run run/
cp /etc/resolv.conf etc/resolv.conf

5-Open another terminal window and type [sudo chroot "hard disk mount point"]
6-sudo apt purge apport at bcache-tools btrfs-progs cloud-guest-utils cloud-init eatmydata libeatmydata1 libfl.*$ libisns.*$ lvm.*$ lxd-agent-loader lxd-installer mdadm motd-news-config open-iscsi pollinate sg3-utils snapd sosreport thin-provisioning-tools unattended-upgrades xfsprogs libtcl8.6 needrestart heimdal$ libnetplan.*$ tpm-udev gsettings-desktop-schemas python3.*$ alsa.*$ bolt finalrd sbsigntool krb5-locales libbrotli.*$ libcbor.*$ libfido.*$ liblvm2cmd.*$ liblzo.*$ libpackagekit.*$ libproxy.*$ libsas.*$ libwrap.*$ libyaml.*$ secureboot-db sound-theme-freedesktop mime-support cryptsetup isc-dhcp-common groff-base glib-networking-common libmpdec.*$ libuchardet.*$ libicu.*$ python-babel.*$
7-sudo apt update && sudo apt-cache search linux-generic
8-sudo apt install linux-image-unsigned-[choose a result from the search]
9-create the file [dhcp.network] in the path /etc/systemd/network/ to be like this
nano /etc/systemd/network/dhcp.network


[Match]
Name=e*

[Network]
DHCP=yes

10-create the file [fstab] in  /ect to be like this
nano /etc/fstab


LABEL=ubuntu	/	 ext4	defaults	0 0

11 - change root password using [passwd] add a user if you want using [adduser [the username]] and add the user to the sudo group using [usermod -aG sudo [the username]]
12- reboot and then open gparted lable the ext4 partion as [ubuntu] and set boot flag to the partition and then run boot repair
